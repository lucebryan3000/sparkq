# TIER 3: PRODUCTION ENVIRONMENT (single container app + Postgres + Redis)
# ⚠️ DO NOT COMMIT ACTUAL VALUES - Use secrets management

COMPOSE_PROJECT_NAME=myapp

# Ports (bridge networking; keep minimal exposure)
APP_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379

# Database (strong passwords required)
POSTGRES_USER=myapp_prod
POSTGRES_PASSWORD= # also written to secrets/db_password.txt
POSTGRES_DB=myapp_prod

# Redis (password required)
REDIS_PASSWORD= # also written to secrets/redis_password.txt

# Connection strings
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}
REDIS_URL=redis://:${REDIS_PASSWORD}@localhost:${REDIS_PORT}

# Application
NODE_ENV=production
LOG_LEVEL=info
NEXT_TELEMETRY_DISABLED=1
