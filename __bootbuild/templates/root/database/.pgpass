# ===================================================================
# PostgreSQL Password File Template
# ===================================================================
# Format: hostname:port:database:username:password
#
# Usage:
#   1. Copy this file to ~/.pgpass
#   2. Set permissions: chmod 600 ~/.pgpass
#   3. Replace placeholders with actual values
#
# This allows passwordless authentication for PostgreSQL commands
# ===================================================================

# Local development
{{DB_HOST}}:{{DB_PORT}}:{{DB_NAME}}:{{DB_USER}}:{{DB_PASSWORD}}

# Wildcard for all databases
{{DB_HOST}}:{{DB_PORT}}:*:{{DB_USER}}:{{DB_PASSWORD}}

# Note: This file should NEVER be committed to git
# Add .pgpass to your .gitignore file
